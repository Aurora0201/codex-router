{
  "name": "codex-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace @aurora0201/codex-router && npm run build --workspace @codex-router/web",
    "cli": "node server/dist/cli.js",
    "dev": "concurrently -n gateway,admin -c cyan,magenta \"tsx watch server/src/index.ts\" \"npm run dev --workspace @codex-router/web\"",
    "lint": "npm run lint --workspace @aurora0201/codex-router && npm run lint --workspace @codex-router/web",
    "pack:check": "npm pack --workspace @aurora0201/codex-router --dry-run",
    "release:check": "node scripts/check-release-metadata.mjs",
    "hash:main-auth": "node scripts/hash-main-auth.mjs",
    "start": "node server/dist/index.js",
    "test": "npm run test --workspace @aurora0201/codex-router && npm run test --workspace @codex-router/web",
    "test:e2e": "npm run test:e2e --workspace @aurora0201/codex-router"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "concurrently": "^10.0.4",
    "tsx": "^4.23.11",
    "typescript": "~7.0.2",
    "vitest": "^4.1.10"
  }
}
